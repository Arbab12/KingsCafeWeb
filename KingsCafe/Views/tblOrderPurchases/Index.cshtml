@model IEnumerable<KingsCafe.Models.tblOrderPurchase>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ORDER_PURCHASE_DATE_TIME)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.tblWorker.WORKER_NAME)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ORDER_PURCHASE_DATE_TIME)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tblWorker.WORKER_NAME)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ORDER_PURCHASE_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ORDER_PURCHASE_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ORDER_PURCHASE_ID })
        </td>
    </tr>
}

</table>
