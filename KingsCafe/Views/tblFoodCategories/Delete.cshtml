@model KingsCafe.Models.tblFoodCategory

@{
    ViewBag.Title = "Delete";
}

<h2 class="text-center">Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>tblFoodCategory</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.FOOD_CATEGORY_NAME)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.FOOD_CATEGORY_NAME)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.FOOD_CATEGORY_PICTURE)
        </dt>

        <dd>
            <img src="@Url.Content(Model.FOOD_CATEGORY_PICTURE)" height="100" width="100" />
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

<div class="form-actions no-color">
    <input type="submit" value="Delete" class="btn btn-danger" /> |
    <a href="@Url.Action("Index","tblFoodCategories") " class="btn btn-primary glyphicon glyphicon-backward"> </a>

</div>
    }
</div>
